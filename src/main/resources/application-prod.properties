
# Use external database in production (example: PostgreSQL)
# spring.datasource.url=jdbc:postgresql://localhost:5432/tedtalkdb
# spring.datasource.username=sdproduser
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# For demo, still using H2 (replace with real DB)
spring.datasource.url=jdbc:h2:file:./data/tedtalkdb
spring.flyway.url=jdbc:h2:file:./data/tedtalkdb

# JPA settings for production
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=validate

# Logging for production
logging.level.org.tedx=INFO
logging.level.root=WARN

# Flyway enabled for initial setup
spring.flyway.enabled=true

# H2 console disabled
spring.h2.console.enabled=false

# Security settings for production
# Use external user store, not in-memory
